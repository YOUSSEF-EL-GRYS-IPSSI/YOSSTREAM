<form method="post" action="{{ path('series_supprimer', {'id': series.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('supprimer' ~ series.id) }}">
    <button class="btn">Supprimer</button>
</form>
