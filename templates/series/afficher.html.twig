
.{% extends 'base.html.twig' %}

{% block title %}Nos films {% endblock %}

{% block body %}
    <h1> mes films </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for series in series %}
            <tr>
                <td>{{ series.id }}</td>
                <td>
                    <a href="{{ path('series_modifier', {'id': series.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">Aucun fichier trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('series_ajouter') }}"> ajouter </a>
{% endblock %}
